package main

func main() {
	var num int = 10 // Explicitly declare the type of the variable
	var hello string = "Hello"
	world := "World" // Implicitly declare the type of the variable (preferred)
	println(hello, world, num)
}
